export class City {
  name: string;
  longitude: number;
  lattitude: number;

  constructor(name: string, longitude: number, lattiutde: number) {
    this.name = name;
    this,longitude = longitude;
    this.lattitude = lattiutde;
  }
}
